# Validation Map

Evidence ledger for the calculation engine. Every ✅/🔶 row cites the vitest
case(s) that verify it — the whole suite (1063 tests) runs in the GitHub
Actions `ci` job on every PR, and the `engine/validation.ts` benchmark IDs
referenced below are additionally rendered side-by-side (manual vs software vs
%diff) on the live `/validation` page.

That page also carries a **Solver engine coverage** section: the full inventory
of the 426 vitest cases across 28 modules that exercise a *solver* — the
model→solver bridge, the FEM solvers, dynamics and stability, and the nonlinear
path followers. It is generated from the test sources by
`npm run gen:coverage`, and `solverCoverage.test.ts` fails if it goes stale, so
the published coverage cannot drift from the suite. It records which cases
exist; CI running the whole suite on every PR is what establishes that they
pass.

**Status legend**
- ✅ verified in CI against the row's stated method (hand calc / closed form / textbook / code table)
- 🔶 verified in CI by an *equivalent* method; the originally-planned external cross-check (ETABS / STAAD / PCA / Excel) is still open
- ⬜ open — needs evidence that cannot be produced from inside this repo (external tool license)

---

# Beam Design

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| B001 | Singly Reinforced | Hand Calc | ✅ | `beamDesign.test.ts` "SRRB — moderate moment stays singly reinforced"; `validation.ts` `rc-beam-mn` (Mn = As·fy·(d−a/2) vs engine) |
| B002 | Doubly Reinforced | Hand Calc | ✅ | `beamDesign.test.ts` "DRRB — Mu beyond φMn_max designs A′s with the displaced-concrete term" + classification consistency |
| B003 | Minimum Steel | Hand Calc | ✅ | `beamDesign.test.ts` "tiny moment falls back to ρ_min" (was planned as Excel; verified by hand formula instead) |
| B004 | Maximum Steel | Hand Calc | ✅ | `beamDesign.test.ts` "ρ_max,TC = (0.85f′c/fy·β1)(3/8)(dt/d)" + "ρ_b carries the dt/d factor" |
| B005 | Multiple Layers | Hand Calc | ✅ | `beamDesign.test.ts` "adds a second layer when one layer cannot fit the bars, lowering d" (§407.7, Varignon) |
| B006 | Large Bar Diameters | ETABS | 🔶 | bar-fit geometry proven in `beamDesign.test.ts` "maxPerLayer honours s_min = max(db, 25)"; ETABS cross-check open |

---

# Column Design

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| C001 | Pure Compression | Hand Calc | ✅ | `columnDesign.test.ts` "axial — tied (review Concrete 7, Problem 2)" vs the published answer key; `validation.ts` `column-phipn` |
| C002 | Uniaxial Bending | Hand Calc | ✅ | `columnDesign.test.ts` "interaction — balanced condition (review Concrete 8, Problem 4 keys)": Pb, Mb, eb vs answer key; Po at large c; φ transition 0.65→0.90 |
| C003 | Biaxial Bending | PCA Column | 🔶 | Bresler reciprocal identity `columnDesign.test.ts` "1/Pn = 1/Pnx + 1/Pny − 1/Po"; PCA Column cross-check open |
| C004 | Slender Column | PCA Column | 🔶 | `columnDesign.test.ts` "slenderness — nonsway moment magnification": 34+12(M1/M2) limit, Euler load, δ ≥ 1, M2,min = Pu(15+0.03h), instability flag; PCA cross-check open |
| C005 | Spiral Column | Manual | ✅ | `columnDesign.test.ts` "axial — spiral (review Concrete 7, Problem 3 / key 2,423.70 kN)" + spiral ratio & pitch limits |

---

# Footings

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| F001 | Centered Footing | Hand Calc | ✅ | `isolatedFooting.test.ts` (sizing, one-way & two-way shear, flexure); `validation.ts` `footing-area` |
| F002 | Eccentric Footing | Hand Calc | ✅ | `eccentricFooting.test.ts` (pressure blocks + worked-solution steps) |
| F003 | Punching Shear | Manual | ✅ | `punchingShear.test.ts` — critical perimeter b0, aspect-ratio & αs branches, all three §22.6.5.2 Vc equations, φVc vs demand |
| F004 | Combined Footing | Textbook | ✅ | `combinedFooting.test.ts` "rectangular (CRF)" + `combinedFootingSolution.test.ts` (printable worked solution) |
| F005 | Trapezoidal Footing | Textbook | ✅ | `combinedFooting.test.ts` "trapezoidal (CTF)" + column-containment checks |

---

# Frame2D

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| FR001 | Simply Supported Beam | Analytical | ✅ | `frame2d.test.ts` "regression vs the beam solver" (closed-form beam engine as oracle) |
| FR002 | Cantilever Beam | Analytical | ✅ | same oracle; 3D twin also closed-form: `validation.ts` `cantilever-defl` / `-moment` / `-slope` |
| FR003 | Portal Frame | Textbook | ✅ | `frame2d.test.ts` "portal frame"; independently cross-checked 2D↔3D in `frame3d.test.ts` (reactions + Mmax agree to 1e-3) |
| FR004 | Continuous Beam | STAAD | 🔶 | `frame2d.test.ts` "NSCP combinations" + beam-solver regression; STAAD cross-check open |

---

# Frame3D

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| F3D001 | Cantilever Column | Analytical | ✅ | `frame3d.test.ts` closed forms in both bending planes (δ = PL³/3EI, M = PL, UDL wL⁴/8EI) incl. Timoshenko δ = PL³/3EI + PL/GAs; `validation.ts` rows |
| F3D002 | Space Frame | ETABS | 🔶 | statics self-checks on the full bridge (ΣR = ΣP, fixed-end moments, shared-LU ≡ per-combo solve) in `frame3d.test.ts` / `modelBridge.test.ts`; ETABS cross-check open |
| F3D003 | Multi-Bay Frame | STAAD | 🔶 | planar portal solved by frame3d ≡ frame2d (`frame3d.test.ts`); grid-model equilibrium in `pipeline.test.ts`; STAAD cross-check open |

---

# Modal Analysis

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| M001 | SDOF | Analytical | ✅ | `modal.test.ts` "SDOF cantilever column — T = 2π√(m/k)" with k from an independent static solve |
| M002 | 2-Story Frame | Textbook | ✅ | `modal.test.ts` "2-storey shear building (Chopra)": eigenvalues k(3∓√5)/2 and ω₂/ω₁ closed form (eigen-solver level; full-frame external check under M003) |
| M003 | 5-Story Frame | ETABS | ⬜ | needs an external-tool model; suite covers period ordering / participation bounds (`modal.test.ts` "generated grid") but no independent 5-storey reference yet |

---

# Response Spectrum

| ID | Case | Method | Status | Evidence |
|----|--------|----------|---------|----------|
| RS001 | SDOF | Manual | ✅ | `responseSpectrum.test.ts` "single mode: CQC = SRSS = Sa·effMass"; spectrum branch anchors (plateau/velocity/floor); `accelSpectrum.test.ts` pseudo-spectral relations |
| RS002 | 3-Story Building | ETABS | ⬜ | RSA machinery verified internally (CQC ≥ SRSS, §208.6.4.2 scaling, storey-shear back-difference identities in `responseSpectrum.test.ts`); ETABS cross-check open |

---

# Engine coverage index

One row per engine area that shipped after this map was first drawn (L9 rule:
the map must not lag the code). "Basis" names the strongest independent anchor
asserted by that file; all run in CI.

| Area | Test file(s) | Basis |
|------|--------------|-------|
| NSCP §208 static seismic | `seismic.test.ts`, `nscpSeismic.test.ts` | every V branch (208-9/10/11) + Ft + w·h distribution vs hand formulas; Method-B caps 1.3/1.4·Ta; accidental-torsion couple statics (ΣΔF = 0, ΣΔF·d = 0.05·L⊥·F); `validation.ts` `seismic-period`/`seismic-base-shear` |
| RSA → design loads | `responseSpectrum.test.ts` | equivalent-load base shear ≡ CQC/SRSS combination (1e-9), single-mode Sa·effMass hand calc, §208.6.4.2 floor scaling |
| Structural irregularities | `irregularity.test.ts` | NSCP Table 208-9/10 thresholds as hand calcs — P1 torsional δmax/δavg (1.2/1.4), V1 soft-storey (0.7/0.6 of above, 0.8/0.7 of avg-3), V2 mass (1.5), V3 vertical-geometric (1.3); real bridge+solver integration (symmetric grid ⇒ regular); `validation.ts` `torsional-irregularity` |
| Load combinations | `loadCombinations.test.ts`, `pipeline.test.ts` | NSCP 203 factor sets as data; Ev = 0.5·Ca·I·D shifts (1.42D/0.68D) |
| Time history | `timeHistory.test.ts`, `timeHistoryModel.test.ts`, `accelerogram.test.ts` | Newmark SDOF vs analytical free/forced responses; modal superposition |
| 3D model → equivalent plane frame | `nonlinearFrameModel.test.ts` | condenses a building by COMBINING all frame lines parallel to the loading direction (perpendicular coordinate collapses; transverse members dropped; EI/EA and Mp summed). **Reproduces the FULL 3D modal T₁ within 10%** for 1-/2-/3-storey frames (measured 0.937–0.969) — independently agreeing with the shear-building reduction's own condensation; total seismic mass preserved exactly; adding a parallel frame line scales stiffness AND capacity by 3/2 as expected; the same condensed frame drives both the static pushover and the hinge time history |
| Nonlinear frame DYNAMICS (hinges) | `nonlinearFrameDynamic.test.ts` | Newmark-β + Newton-Raphson over the frame DOFs sharing `assembleFrame` with the static driver: with Mp = ∞ it reproduces `newmarkDirect` on the same K/M/C to 1e-9 and converges in one iteration; a hinge DOF is added per hinged member end; weak record stays elastic; yielding cuts base shear below the elastic demand and caps every hinge moment; plastic-rotation demand grows as Mp falls (energy deliberately NOT asserted — non-monotonic in Mp, same as nonlinearTimeHistory) |
| Displacement control (post-peak) | `nonlinearFrame.test.ts` | prescribes the control displacement and SOLVES for λ (δd = δd_R + δλ·δd_P, constraint fixes δλ), so the capacity curve can descend past its peak: hits the prescribed displacement every step, matches the elastic closed form to 1e-3, HOLDS the collapse load flat at Mp/L = 33.34 over a 6× drift range where load control instead runs displacement to >10 m, and traces a monotonically DESCENDING branch when the hinge softens (b < 0) — which load control structurally cannot produce |
| Arc-length (Riks/Crisfield) path following | `arcLength.test.ts` | prescribes the step LENGTH in (d, λ) space and solves for both, passing the load limit point on the sign of det(Kt) rather than on a guess. Elastic path exact (λ = |δ|·3EI/L³ to 1e-9); ‖Δd‖ = Δl verified per step; halving Δl doubles the step count with both endpoints still on the exact line; **peak λ = Mp/L to 4e-14 %** for a perfectly plastic hinge (`validation.ts` `arc-length-collapse`) where load control can only bracket it; collapse load held flat within 5% over a 2× drift range; monotonically descending branch for b < 0; **coincides with displacement control point-by-point to 1e-3 relative** (the residual is the reference's own interpolation across the knee); yield-event step trimming and arc cutting exercised. Snap-back: a real reversal (λ and \|δ\| falling together) is traced for \|b\| > 3, but see the module scope note — it is arc-size dependent and NOT robust |
| Smooth hinge backbone (C^∞) | `smoothHinge.test.ts` | f = b·k0·u + Fp·tanh(kp·u/Fp) — the bilinear law's two asymptotes with the corner rounded. Verified: slope k0 at the origin, post-yield asymptote to 1e-12, matches `bilinearProbe`'s virgin backbone to 1e-6 away from the knee, the elastic softness vanishes as the tanh series x²/3 (100× drop per 10× smaller u, leading coefficient checked), reported tangent IS df/du by central difference through the knee, tangent continuous where the bilinear one jumps the FULL k0→b·k0 swing in a single sample, tangent bounded by the two asymptote slopes, softening hinge passes smoothly through zero tangent; nonlinear-elastic character pinned (odd function, zero net work round a full cycle, no permanent set) |
| Robust snap-back (smooth material) | `arcLength.test.ts` | `material: 'smooth'` + residual convergence + backtracking line search + knee-resolving step control: **hundreds of sustained snap-back reversals** for \|b\| > 3 (vs 3 at best on the bilinear material) with no step failure, and the traced path is **independent of the arc length** — peak λ and peak δ agree across a 4× change in Δl for b = −4/−6/−12. Mild softening still gives a monotone descending branch and no false snap-back. A perfectly plastic hinge reaches the EXACT collapse load on both materials (`validation.ts` `arc-length-smooth-collapse`); only where the peak sits at the knee (softening) does rounding shave it, always low, <1% |
| Model → biaxial frame + SKEW pushover | `biaxialFrameModel.test.ts` | 1:1 bridge (no plane-frame condensation — collapsing the perpendicular coordinate is exactly what the coupled surface cannot survive), with section properties inherited from `modelToFrame3D` rather than re-derived, plus a weak-axis plastic capacity to pair with the existing strong-axis one. Verified: `weakPlasticMoment` = Fy·Zy for a W-shape, = the strong-axis formula on the b/h-swapped section otherwise, equal on a square and strictly lower on a deep one; members map 1:1 with E/G/A/Iy/Iz/J/rot **identical** to the linear bridge; releases and offsets the hinge element cannot represent are REPORTED, never silently dropped. Pushover at any plan angle: pattern normalised so λ IS the base shear; **every step converges with residual < 1e-8**; **plan symmetry 0°/90°/180° agree to 1e-12** on a 4-fold symmetric frame (`validation.ts` `biaxial-plan-symmetry`); elastic mode traces a straight line; the diagonal push lands between the principal-direction capacity and √2× it, because both orthogonal frame lines are recruited; and **weakening the coupling (α = 2 → 4) buys diagonal capacity while changing the 0° answer by < 1e-9**, which is the frame-level proof that the coupling — not some incidental asymmetry — is what limits the skew case. **Capacity is invariant to the hinge penalty rigidity over 1e2–1e4**: that check caught a real defect, where too shallow a backtracking depth left the push non-converged at high rigidity and reporting 287 kN instead of 405 kN. `summarizeBiaxialPushover` condenses a run into headline numbers plus PLAIN-LANGUAGE cautions (dropped releases/offsets, non-converged steps, mechanism, nothing hingeable) — pure and tested, so the Model Space panel renders judgements it does not make; tests check the ranking, the limit, and that a dropped release warns the reader the run is 'stiffer and stronger' than the real frame rather than merely noting it happened |
| Space frame with BIAXIAL hinges | `nonlinearFrame3d.test.ts` | 6 DOF/node plus TWO extra rotation DOFs per hinged end, taken in the member's LOCAL axes so the hinge deformation is the node's global rotation vector projected onto y′/z′ (B) minus the beam-end DOF; the element is frame3d's own `kLocal` wired through a rectangular transform A. Verified: **reproduces `solveFrame3D` to 1e-12** with no member hinged (A degenerates to frame3d's T); elastic cantilever formulas on BOTH bending axes; an unyielded hinge softens the tip by exactly 3/rigidity, a predicted number rather than a bound; two DOFs added per hinged end and none for an unhinged member; torsion stays continuous across a hinge. Collapse: λpeak = Mpz/L and Mpy/L uniaxially, agreeing with the 2-D `nonlinearFrame` on the same problem to 1e-6; **collapse load independent of load angle on a circular surface** (uncoupled 1-D hinges would gain √2 at 45°); **matches the elliptical closed form 1/(L·√((sinα/Mpy)²+(cosα/Mpz)²)) at every skew angle** (`validation.ts` `biaxial-frame-skew-collapse`); **invariant to an arbitrary rigid 3-D rotation of the whole model to 1e-6** (three Rodrigues rotations) and the elliptical closed form still holds for a skewed member expressed in its own derived local axes — the tests that the projection in B and the transform in A agree. P–M reduces the collapse load once past AISC's 1.18(1−P/Py) cap plateau; hinges unload elastically and re-yield under reversal. Convergence is **residual-based**: ‖Δd‖/‖d‖ cannot distinguish convergence from divergence, and a fully plastic biaxial hinge leaves a genuine zero-stiffness direction that made an increment test certify a run-away state as converged |
| T-section gross & cracked properties | `memberDeflection.test.ts` | a monolithic beam and its slab form a T for STIFFNESS, and the true section sits between the two rectangles people substitute for it — the web-only rectangle (0.61·Ig, the conservatism this replaces) and a full-depth rectangle of width bf (2.0·Ig, rejected in #446). `tSectionGross` verified against the transfer-axis identity I_c = I_top − A·ȳ² computed by different algebra (`validation.ts` `tbeam-gross-inertia`), and shown to degenerate EXACTLY to the rectangle for bf ≤ b, hf = 0 and hf ≥ h. `crackedInertiaT` is the bf-rectangle while the neutral axis stays in the flange, the WEB rectangle when hogging (the flange is cracked in tension — a T-beam over a support is a rectangular beam), and matches an INDEPENDENTLY BISECTED neutral axis when it drops into the web. Threaded through `pipeline` from the same effective flange the design already uses: a flanged member is stiffer, cracks later (higher Mcr) and deflects less; Mcr takes yt from the tension face, which the governing moment's SIGN now selects |
| Laterally loaded piles (Broms + p-y) | `lateralPile.test.ts` | **Broms** ultimate capacity, short (soil) and long (hinge) branches, clay and sand, free and fixed head — the governing answer is always the lower branch, which is also what tells you which kind of pile it is. Clay short free-head is DERIVED not quoted, and reproduces the classic H = (√2−1)·q·L (`validation.ts` `broms-clay-short`); sand short free-head matches 0.5·Kp·γ·d·L³/(e+L) built independently from the resultant and its lever arm (`broms-sand-short`); fixed-head clay equals the full effective length in bearing; the sand long-pile root back-substitutes into its own hinge condition to 1e-4; capacity falls with load height and rises with length, strength, diameter and φ. **p-y curves**: Matlock reaches pu at exactly 8·y50 and half of pu at y50, is odd and monotone, and caps at N = 9 with depth; API sand coefficients come from the analytical wedge expressions (not a chart fit), increase with φ, and the curve saturates at A·pu with initial slope k·z. **p-y solve** (beam elements on nonlinear springs): converges at every load from 10 to 400 kN in under 20 iterations at 1e-12, mesh-converged to 0.1% at 20 elements over 12 m, deflection monotone and softening in load, stiffer soil/pile and a fixed head all reduce it, and a pile past its active length stops caring about extra length. **Two defects the tests caught**: the head-moment sign was inverted, so an eccentric load made the pile deflect LESS (the API now states the moment physically and negates internally); and Matlock's cube-root curve has an infinite tangent at y = 0, which stalled every service-load solve at a residual near 2e-6 regardless of iterations — regularised below 1e-3·y50 (~0.015 mm), after which the same cases converge in 5–9 steps |
| Foundation settlement (stress, elastic, consolidation) | `settlement.test.ts` | **Boussinesq** corner factor against published table values (0.1752 / 0.0840 / 0.1999 / 0.1431 at 4 dp — the precision the reference carries), symmetric in m and n, monotone, and **→ 0.25 as m, n → ∞**, which is the check on the arctangent branch shift that would otherwise silently halve the factor for large areas; centre = four superposed quarter-rectangles exactly; stress decays with depth, never exceeds q, and **exceeds the 2:1 spread while converging toward it** (55% → 22% over z = B/2 → 1.5B) because 2:1 is an AVERAGE and Boussinesq-centre is the PEAK. **Effective stress** with a water table verified against hand sums with pore pressure removed. **Consolidation**: matches the closed-form Cc·H/(1+e₀)·log₁₀(σ′f/σ′₀) to machine precision on one slice (`validation.ts` `consolidation-nc`); an overconsolidated layer settles exactly Cr/Cc = 1/6 of the NC value; the **recompression+virgin split** when Δσ crosses σ′p matches its two-term hand calc and lies below the pure-NC answer — getting that split wrong is the classic several-fold overestimate on a crust; slicing convergence demonstrated; layers above founding level excluded. **Time rate**: Tv(90%) = 0.848 (`validation.ts` `consolidation-tv90`), U ↔ Tv inverts to 1e-6, single-face drainage takes exactly 4× as long, and the tests pin the fit's OWN inaccuracy — 0.1963 vs the tabulated 0.197, and a 1.3% step between branches at U = 60% — so neither is later mistaken for exact. **Schmertmann** peak influence below the footing (2B square / 4B strip), C₁ depth relief floored at 0.5, C₂ creep scaling exactly, sublayers below the influence depth ignored |
| Biaxial (P–My–Mz) plastic hinge | `biaxialHinge.test.ts` | vector return mapping with kinematic hardening on a coupled yield surface, so the two bending axes are no longer treated as independent 1-D hinges. Verified: **degenerates to `bilinearProbe` to 1e-9** over a full cyclic path both with the other axis capacity-free AND with the full surface active but unloaded (catches spurious coupling); initial tangent is diag(k₀y, k₀z) and unloading snaps back to k₀; the motivating case — 0.8Mpy **and** 0.8Mpz, which two 1-D hinges call elastic — is correctly past yield; plastic flow is normal to the surface (associativity, cross product 1e-10); **the consistent algorithmic tangent matches central finite differences of the return map itself to 1e-5 relative** on all three surfaces and is symmetric to 1e-9. Surfaces: Orbison Eq. 10.18 hits Mpy/Mpz exactly at p = 0, matches the **closed-form contour** √((1−1.15p²)/(1+3.67p²)) at p = 0.3 (`validation.ts` `biaxial-orbison-contour`), shrinks monotonically in p and squashes at p = 0.933 as the published fit does; Bresler power contour reproduced at α = 1.5 with the |m|^α regularisation shown to shift the surface by <1e-5. 45° skew push projects to Mp/√2 (`validation.ts` `biaxial-skew-projection`). Return map converges with |Φ| < 1e-9 at every angle of a 32-point skew sweep on all three surfaces; a hinge whose capacity axial load has driven to zero degrades to the hardening branch instead of producing NaN |
| P–M interaction on hinge capacity | `nonlinearFrame.test.ts`, `nonlinearFrameModel.test.ts` | hinge yield moment reduced to Mpc(P) via `reducedPlasticMoment`, with P recovered from the member's current axial state each iteration: a portal frame yields EARLIER with P–M than without (λ 108.3 vs 112.3 — the unconservative case it fixes), column capacities drop below Mp while the beam (no P–M data) keeps its own, the two columns recover equal-and-opposite axial forces, a smaller Pcap yields earlier still, and the bridge supplies Pcap + a material-appropriate surface automatically, summing capacity across combined parallel frames |
| Distributed member-end plastic hinges | `nonlinearFrame.test.ts` | concentrated plasticity with the hinge rotation as an explicit DOF (node θ —[spring]— θb ═ beam), so state determination is exact. Elastic limit reproduces δ=PL³/3EI to 1e-12 and a rigid unyielded hinge changes it by <0.1%; **rigid-plastic collapse loads recovered**: cantilever P=Mp/L and fixed–fixed central load P=8Mp/L (`validation.ts` `plastic-collapse-fixed-beam`, bisected so it is not step-size limited); cyclic response leaves a permanent set, dissipates energy, and yields in BOTH directions — which the event-to-event pushover hinge (a permanent release) cannot represent  **Multi-hinge convergence (fixed)**: the driver now converges on the RESIDUAL, not the displacement increment, and backtracks on it. Plain full Newton with an increment test converged NONE of 30 steps on a 3-bay × 4-storey frame with 56 hinges — the hinge tangent drops by ~k0/(b·k0) at yield, so the full step is wildly wrong once many hinges yield together, and ‖Δd‖/‖d‖ cannot tell a stalled iteration from a converged one. Now every step converges below 1e-8, and the peak load is **step-count independent** (30 vs 60 increments agree; while the steps were failing it was under-reported by 0.2%). `NLFrameStep` reports its residual per the L5 convention. The DYNAMIC driver was measured, not assumed: it converges in 3 iterations on the same frame at three shaking intensities, because Newmark's a0·M term regularises the tangent — so it was left alone |
| Nonlinear TH — frame reduction | `nonlinearModel.test.ts` | the equivalent shear building reduced from a 3D frame reproduces the FULL modal T₁ within 10% for 1-/2-/3-storey frames (0.937–0.973 of modal); storey stiffness (secant V/Δ, same definition as the soft-storey irregularity check) decreases with height; capacity Σ2Mp/h scales inversely with storey height; elastic reference run never yields, strong record yields and dissipates, yielding caps base force below the elastic demand |
| Nonlinear TH (hysteretic) | `nonlinearTimeHistory.test.ts` | Newmark + Newton-Raphson on hysteretic springs reduces EXACTLY to `newmarkDirect` when nothing yields (1e-9, 2-DOF) and converges in one iteration there; bilinear kinematic-hardening material — yields at Fy, post-yield slope b·k₀, **elastic unloading at k₀** and reverse yielding (what the event-to-event pushover hinge cannot represent); loop energy 4·Fy·(A−u_y); base force capped at Fy; ductility demand monotone in Fy; `validation.ts` `hysteretic-loop-energy` |
| Direct-integration TH (Rayleigh) | `directTimeHistory.test.ts` | full-MDOF Newmark ≡ mode-by-mode superposition to 1e-9 on a 2-DOF shear building (Rayleigh C is diagonalized by the modes, so the two paths are algebraically identical); 1-DOF ≡ `newmarkSDOF` to 1e-10; free-vibration log decrement exp(−2πζ/√(1−ζ²)); Rayleigh α/β inversion hits both target ζ exactly, sags between anchors; `validation.ts` `direct-th-decay` |
| Tension/compression-only members | `axialOnly.test.ts` | active-set iteration on a cross-braced bay: exactly one diagonal deactivates, the survivor is in tension (compression for comp-only struts), load reversal swaps WHICH diagonal is active, the braced bay is stiffer than the unbraced one, re-activation prevents latch-off, converges in ≤5 iterations, equilibrium ΣR=ΣF still holds after a member drops; the converged solve is BIT-IDENTICAL to a separate model with the slack diagonal deleted (`validation.ts` `brace-active-set`, 1.7769 mm both ways); `meshValidation` rejects an axial release on a limited member and warns on nodes held only by limited members |
| Per-combo active set (NSCP combos) | `axialOnly.test.ts` | `analyzeActiveSet` reproduces `analyzeFrame3D` combo-for-combo when no member is limited (govIdx, names and Mmax to 1e-9) and reports one converged active set per combination otherwise; the gravity combo and the seismic combos settle on DIFFERENT sets, proving the shared-LU sweep would be invalid; superposition is explicitly shown to fail (D-only + E-only ≠ the combined solve, because the parts settle on different sets); every combo satisfies ΣR + ΣF = 0 on its own set |
| Dashed-overlay geometry (viewport) | `dashPattern.test.ts` | `dashSpans` splits a member into an ODD cell count so a dash lands at each end, is symmetric about mid-span, honours the fill fraction (gaps really exist), floors to `minCells` on short members and tracks the requested pitch on long ones, and keeps every dash inside the member — the 3D overlay's only non-trivial maths, tested as a pure module because the WebGL canvas cannot be screenshot-verified |
| Buckling | `buckling.test.ts` | linearized Pcr vs Euler closed forms (cantilever, fixed-fixed) |
| Pushover | `pushover.test.ts`, `pushoverModel.test.ts` | event-to-event capacity curve vs hand-tracked hinge sequence (review-problem anchors) |
| Floor vibration | `floorVibration.test.ts` | AISC DG11 fn = 0.18√(g/Δ) + tolerance thresholds |
| Shells | `shell.test.ts`, `frame3dShell.test.ts`, `shellModel.test.ts` | CST/DKT element checks + frame↔shell model consistency |
| Steel member design | `steelDesign.test.ts`, `aiscSections.test.ts`, `flexure.test.ts`, `shear.test.ts` | §F2/§F3/§F6/§G2.1/§E3/§H1-1 formula re-derivations; `validation.ts` `steel-phimp`/`steel-f3-flb`/`steel-phivn`. §F4/§F5 (noncompact/slender web) and §F9 (tees) are OUT OF SCOPE and refused — see AUD-001 §9 |
| Steel connections | `steelConnections.test.ts`, `boltedConnection.test.ts`, `weldedConnection.test.ts`, `connectionSolution.test.ts`, `baseplate.test.ts` | IC-method bolt/weld groups vs `validation.ts` `bolt-ecc-rmax`/`weld-ecc-fmax`/`bolt-oop-tension`/`prying-t0`; AISC DG1 base plates |
| Effective length K | `effectiveLength.test.ts` | alignment-chart G-factors vs published values (review anchors) |
| Timber (wood) member design | `woodDesign.test.ts` | NDS §3 / NSCP §6 ASD adjustment factors (CD/CM/CF/CV), beam CL (§3.3.3) + column CP (§3.7.1) closed-form anchors, beam-column §3.9.2 interaction; `validation.ts` `wood-cp`/`wood-cl` |
| Timber wood slab (deck + joists) | `woodSlab.test.ts` | NDS §3 / NSCP §6 ASD deck-on-joist floor: simple/continuous UDL coefficients (wL²/8, wL²/10), f_b = M/S, service deflection 5wL⁴/384EI (L/360 live, L/240 total), repetitive-member Cr, board-feet take-off, bamboo-slat option; `validation.ts` `wood-slab-joist` |
| Slope stability (method of slices) | `slopeStability.test.ts` | Fellenius FS vs a documented 3-slice hand calc (FS = 2.102); Bishop ≥ Fellenius; Janbu f0 > 1; circle-slice geometry (chord width, symmetric α); critical-circle search FS sanity + steeper-slope monotonicity; `validation.ts` `slope-slices-fellenius` |
| Plumbing — water supply (RNPCP 2000) | `plumbingFixtures.test.ts`, `waterSupply.test.ts` | Table 6-5/7-2 fixture-unit totals vs Module 2/3/4 worked examples; demand (ΣFU×8), static head, continuity velocity, Hazen-Williams friction; `validation.ts` `plumb-velocity`/`plumb-friction` |
| Plumbing — drainage/DWV (RNPCP 2000) | `drainage.test.ts` | Table 7-5 drain/vent sizing + max lengths vs Module 3 examples (14 DFU→76/51 mm, 39 DFU→102/65 mm); vent ≥ max(32, drain/2); §1206 slope; `validation.ts` `plumb-drain` |
| Plumbing — septic tank/OSST (RNPCP 2000) | `septicTank.test.ts` | Table B-2 capacity by DFU (+94.6 L/FU over 100); Appendix B two-chamber layout (2/3·1/3, freeboard, depth 0.6–1.8 m) vs Module 4 example (78 DFU→11,355 L→2.0×4.8×1.5 m); `validation.ts` `plumb-septic` |
| SCWB | `scwb.test.ts` | ΣMnc/ΣMnb ≥ 6/5 (§418.7.3.2) with hand Mn |
| Beam moment-strength ratios | `beamMomentRatios.test.ts`, `beamDesign.test.ts`, `beamCage.test.ts` | §418.6.3.2 (SMF, ½ and ¼) and §418.4.2.2 (IMF, ⅓ and ⅕), checked on the PLACED bars rather than on the schedule — only a cut knows what BOTH faces have at a station, because the bottom steel at a support is whatever ran through from midspan and no section design sees that. `sectionStrength` takes d to the CENTROID of each face's steel, so a second layer lowers the lever arm exactly where the ratio is tightest (asserted against a single-layer cut with the same As). Both sentences proven to BITE on synthetic cages: a 4-⌀25 top / 2-⌀16 bottom beam fails the at-face rule under SMF, a balanced one fails at ½ and passes at ⅓, and an early-curtailed bar fails the along rule on Mn+ while the at-face rule passes. Then end to end on a designed frame: an 8 m 350×650 SMF bay under 14 kPa SDL + 20 kPa LL used to come out at Mn+/Mn− = 0.31 (7-⌀20 top against the §409.6.1.2 minimum on the bottom face, because the sagging demand at a support is nil under gravity), and now passes at a tightest utilisation of 1.03 — governing, which is what proves the rule set the bar count rather than something else clearing it. Two mechanisms carry it and each is tested on its own: `BeamDesignInput.AsFloor` (raises As, never lowers it, beats §409.6.1.2 when larger, leaves ρ ≤ ρmax) and `BeamCageInput.system`, which replaces the §409.7.3.8 continuous shares (⅓ top, ¼ bottom) with the larger seismic one — counted from the DRAWN bar paths reaching the joint face, and clamped so the cage never claims steel the face has not got. IMF asserted cheaper than SMF in total bottom area. Frame elevation rendered in headless Chromium both ways: the gravity sheet's support cut reads 7-⌀20 T, 2-⌀20 B with the bottom bars dimensioned as starting 1283 from the face; the SMF sheet reads 7-⌀20 T, 4-⌀20 B with that curtailment dimension gone, because every bottom bar is now continuous |
| Slabs | `slabDDM.test.ts`, `woodArmer.test.ts`, `slabDeflection.test.ts` | DDM coefficient tables; Wood–Armer moment transformation identities |
| Slab openings (trimmer bars) | `slabOpening.test.ts`, `planDetails.test.ts` | §408.5.4.2 replacement rule against a documented worked example (6.0 × 5.0 m panel, ⌀12 @ 200 mat, 1.00 × 0.80 m opening): bars interrupted taken as the PHASE-INDEPENDENT upper bound ⌊cut/s⌋ + 1 (5 in x, 6 in y — the rounded quotient would print one short on half of all set-outs), half each side rounded UP so 2·n_side ≥ n_interrupted for every count 0–40, replacement bar = opening + ℓd each face (1729.5 / 1529.5 mm) with ℓd re-derived independently in `validation.ts` `slab-opening-trimmer`, diagonal corner bar floored at 1000 mm when 2·ℓd is shorter; the §408.5.4.2(b)/(c) ⅛ and ¼ strip limits fire on an oversized corner opening and the §422.6.4.3 4h clearance on a small one, each proven to FAIL rather than asserted vacuously. The sheet itself is checked for what a primitive assertion misses — every text extent inside the sheet bounds, every drawn bar inside the panel, each diagonal PERPENDICULAR to the corner crack it closes — and was rendered in headless Chromium at each iteration |
| Wall junctions (corner / intersection / construction joint) | `wallDetail.test.ts`, `planDetails.test.ts` | §422.9 shear friction against a hand calc (3.00 × 0.200 m web, Vu 400 kN, roughened μ = 1.0 → Avf 1285 mm², `validation.ts` `shear-friction-avf`), with the Table 422.9.4.2 μ set asserted term by term and the roughened-vs-not ratio shown to be exactly 1/0.6; the §422.9.4.4 ceiling re-derived from all three limbs and proven to be a property of the INTERFACE (Vu 2000 kN fails at φVn,max 1890 kN however much steel is added), the §422.9.4.5 permanent-compression credit, and Avf floored at zero. Wall steel: Table 411.6.1 minimums including the Grade-415 case that does NOT meet the fy ≥ 420 row (ρt 0.0025, not 0.0020 — the same threshold `slabDDM` uses), §411.7.2.1/§411.7.3.1 spacing at the lesser of 3t and 450, §411.7.2.3 two curtains past 250 mm, all asserted on both sides of their thresholds. Detailing: corner-bar leg = Class B lap (§425.5.2) verified on the DRAWN path geometry, and the §425.4.3 hook checked for FIT — a ⌀12 hook needs ℓdh 261 mm and a 200 mm wall offers 180, so the intersection sheet draws a lapped U-bar instead of a hook that cannot be built, with the hook branch covered separately on a 300 mm wall. That embedment comes from the shared `hookEmbedmentAvailable`, the same helper the column asks, but a WALL deducts the clear cover ONLY — no hoop crosses it and the bend turns against the far curtain rather than behind a column vertical — so `ldhClear` = cover and the drawn leg is placed at it, asserted on the path's own y. Sheets rendered in headless Chromium each iteration; the shared-result note leaking the hook warning onto the corner sheet was found that way and is now a test |
| Beam–column joints (§418.8) | `beamColumnJoint.test.ts`, `planDetails.test.ts`, `planSheets.test.ts` | the joint neither member design checks. **§418.8.2 is asked as two rules, not one** — a correction from review: §418.8.2.3's 20db joint depth is conditioned on reinforcement *extending through* the joint (a bond/slip rule for a bar pulled one way on one face and pushed the other way on the other), while bars *terminated* in the column are governed by §418.8.2.2 and §418.8.5. Tested both ways on the same bar: a ⌀28 hooked into a 400 mm column does NOT fail 20db (the rule does not reach it) but DOES fail its anchorage; the same bar continuing through an interior joint fails 20db at 560 vs 400; and a ⌀25 spandrel passing through is measured against the column WIDTH, not the depth. Demand from the joint FREE BODY, Vu = T + C − Vcol with the steel at 1.25fy (§418.8.2.1) — C equal to the far beam's own bar tension by section equilibrium and zero at an exterior joint, asserted rather than collapsed into a single product. Table 418.8.4.3 γ term by term with φ = 0.85 (§421.2.4.3, not the 0.75 of member shear); effective width as the least of b+h, b+2x and the column width including the eccentric case; §418.8.5.1 ℓdh shown shorter than the general §425.4.3 hook, floored at 8db/150, and carrying `ldhInputs` so the printed number can be checked against its own fy/db/f'c/λ. The embedment it is measured against is the core INSIDE the far-face column vertical the hook turns down behind — colH − cover − hoop Ø − column bar Ø — so the ⌀28 in the 400 column has 330 mm, not 350; a 250 × 250 column with ⌀10 ties and ⌀16 verticals leaves 184 mm against a ⌀16 beam bar's 269.6 mm and fails, hand-checked both ways. `validation.ts` `joint-shear-ratio`. Two-view sheet rendered in headless Chromium; it draws hooks only where bars terminate and straight bars where they pass through, with the anchorage notes dropping with them. The sheet PLACES the hook at `ldhClear` = cover + hoop + column bar and both its callout and its note quote that same number — it used to draw the bend at cover + 60 (100 mm) under a note reading 60, disagreeing with itself and with the beam elevation for the same joint; asserted now on the drawn path's own x. The two hooks are separated by a bar Ø so their 12db tails do not merge into what reads as a closed loop, and the ℓdh dimension carries a white mask so it stops overprinting the hoops it is drawn among |
| Column cross-section (tie / crosstie hooks) | `columnSection.test.ts` | the drawn hook legs are §425.3.2 lengths, not drawing constants: the 135° corner hook and the crosstie stubs were `rw * 1.6` and `stub * 1.9`, so the section read correctly as a diagram while a ⌀10 tie drew a 27 mm leg where the detail calls for 75. Now max(6·dt, 75) to scale — the 75 floor asserted on a ⌀10 tie, 6·dt governing on a ⌀16, the leg proven to scale with the DRAWING (half the size, half the leg) rather than with the bar radius, and a crosstie stub on a 150 mm column proven to stay inside the concrete rather than being drawn past the bar it returns to |
| RC misc | `devLength.test.ts`, `torsionDesign.test.ts`, `beamDeflection.test.ts`, `shearWallDesign.test.ts` | §425.4 ld, §422.7 threshold/cracking torsion, Branson Ie, wall shear. **Hook FIT** (`hookFit`): whether there is room in the member for the hook the bar needs, which the four lengths cannot answer on their own — embedment = depth − cover − tie Ø − far-face bar Ø, because the hook turns down BEHIND that bar (250 − 40 − 10 − 16 = 184 mm, not 250 and not the 200 mm that stopping at the tie gives). Tested on both sides of the boundary, at zero room in a member thinner than its own cover, and for the remedy: the reported `depthNeeded` is the depth that turns the check, since a longer TAIL cannot — ℓdh is measured to the outside of the bend and the 12db tail runs across it. Shared with `beamColumnJoint`'s `ldhAvail` so the joint sheet and the dev-length page cannot quote different room for the same bar |
| Frame elevation — label collisions | `frameElevation.test.ts` | the TOP and BOT. face callouts shared one baseline and relied on being sent to opposite sides of it to stay apart. `angledLeader` flips a side whose label would leave the bay, and a deep beam makes the BOT leader's 45° leg long enough that its preferred side always overruns — so both landed on the same side of the same line and printed through each other on EVERY sheet of every frame, gravity and seismic alike (8 overlapping pairs per frame at four different load/system combinations, e.g. "3-⌀20 TOP THRU + 8 EXTRA" over "4-⌀20 BOT. THRU" on an 8 m bay, their x-extents starting 8 mm apart). They are stacked on their own lines now, one glyph height apart, so the horizontal clamp is free to put each wherever the bay allows and they cannot collide. Asserted on the DRAWN text's own boxes — full advance width, cap height up, a short descender down — rather than on the baselines, because separate baselines are the fix and testing for them would only be testing that the fix is present; the check is run over every sheet of the reference frame and of the deep 350×650 SMF frame that broke it, and was confirmed to report the defect before the change and none after. Rendered in headless Chromium both ways |
| Beam reinforcement elevation (ℓdh at a hooked end) | `beamDetail.test.ts` | the sheet used to draw the end hook 60 mm inside the far face and never ask whether the bar it drew could be developed. Given the supporting column it now dimensions the anchorage from `endHookAnchorage`, using the §418.8.5.1 ℓdh — the SAME clause the joint sheet prints, so one bar in one column cannot carry two different hook lengths on two drawings. Hand-checked: ⌀16 beam bar into a 250 column with ⌀10 ties and ⌀16 verticals → 184 mm available against ℓdh 269.6, drawn as `184 AVAIL / ℓdh 270 REQ` with an `ℓdh 86 SHORT` warning line and a note naming the depth that would work (336) and stating that lengthening the tail does not count; the same bar in a 600 column reports 534 available, no warning geometry and no warning colour. The hook is placed behind the real cage (66 clear = 40 + 10 + 16, not the nominal 60), and the support is drawn at the column dimension parallel to the bars. Absent a column the sheet still builds and simply does not dimension the hook |
| Cracked service deflection in model space | `memberDeflection.test.ts`, `pipeline.test.ts` | §424.2 by DOUBLE-INTEGRATING the member's own FEM moment diagram (piecewise-linear curvature integrated in closed form), so the load pattern and end restraint come from the model, not a tabulated coefficient. Anchors: SS UDL 5wL⁴/384EI, fixed–fixed wL⁴/384EI, SS central point load PL³/48EI and cantilever tip PL³/3EI both **exact** (linear M), cantilever UDL wL⁴/8EI, O(Δx²) convergence demonstrated by a 4× error drop per halving; against a real `frame3d` run the integration reproduces the SOLVER's own nodal deflection (fixed–fixed PL³/192EI to 1e-6) and resolves the propped-cantilever peak the nodes miss (wL⁴/185EI at 0.5785L, within 1%). Branson Ie bracketed by Ig/Icr, λΔ = ξ/(1+50ρ′) relieved by A′s, `validation.ts` `beam-defl-integration` |
| Minimum flexural steel — two clauses, and which one bites | `flexure.test.ts`, `foundationSolution.ts` | §9.6.1.2 is the BEAM minimum on b·d; §24.4.3.2 is the shrinkage minimum on the GROSS section b·h, and §13.3.2.1 sends footings to that one. `flexuralSteel` applied the beam rule alone and was never even told h, so the slab rule could not enter the comparison. Both are computed now and the default is the GREATER — which is what most offices detail to, is what the engine already effectively produced, and is the reading that cannot be wrong in the unsafe direction — with `AsMinBasis` letting a user take the §13.3.2.1 slab rule alone or the beam rule alone. No existing design moves: on the worked footing the beam rule is 3475 mm² against the slab rule's 2400, so `max` returns exactly what was returned before. A second finding fell out of writing it: ρ_sh is BANDED on fy (Table 424.4.3.2) and 0.0018 is the Grade-420 row, not a constant — below 420 MPa it is 0.0020, so "0.0018·A_g" applied to Grade 415, which is what the page footnote said, is 10% light. Asserted across the bands including the 0.0014 floor above Grade 420, on both sides of the 420 threshold, and with a case where the slab rule genuinely governs (a thick raft where h ≫ d). The worked solution prints both candidates and names which was larger instead of a bare unexplained floor |
| One-way shear — six engines, six copies of §422.5.5.1 | `shear.test.ts`, `beamDesign.test.ts`, `retainingWall.test.ts`, `torsionDesign.test.ts`, `workedSolutionPreview.test.ts` | Fixing the Vc coefficient in `shear.ts` reached every footing and NO beam: `beamDesign`, `combinedFooting`, `retainingWall` and `torsionDesign` each carried their own inline `√f'c/6`, so the model-space pipeline — which designs every beam through `beamDesign` — never saw it. All four now route through the shared `oneWayVc` (`combinedFooting` inverts it for d, so it carries the coefficient rather than the call). `punchingShear` was already on the decimal form and `prestressedBeam` already used 0.17/0.29. Verified end to end: a beam designed through the full pipeline reports a Vc identical to the shared expression to 1e-9, where the old /6 form differs by 2%. Three tests pinned the fractions as their expected values and now assert the printed coefficient; a fourth — `workedSolutionPreview`, which exists to stop the landing page advertising arithmetic the engine no longer produces — caught the demo constants drifting and they were regenerated from the engine (Vc 116.4 → 118.7 and everything downstream of it) |
| Punching critical section at a free edge (§22.6.4.1) | `shear.test.ts`, `punchingShear.test.ts`, `isolatedFooting.test.ts` | Reported as "the isolated-footing drawing does not reflect a column that is not on centre", which turned out to be the visible end of a modelling gap: `position` (interior/edge/corner) set αs and NOTHING else — same B, same Dc, same depth for all three, and the column drawn centred in every case. The three αs values exist precisely BECAUSE the critical section loses sides at a free edge, so claiming the edge condition for αs while keeping a four-sided perimeter is internally inconsistent: `punchingDepth` was using a perimeter 44% (edge) and 126% (corner) longer than the section has — unconservative — while αs, not being the governing Vc expression in the common case, made no difference at all. `criticalSection` is now the single source for bo and Ao, truncating in x for an edge and in x and y for a corner, and the standalone punching page routes through it too: that page HAD the truncation but transposed, computing `c/2 + d` where the section gives `c + d/2` (the section runs d/2 past the far column face and stops AT the free edge, so an edge halves the d term, never the column dimension), giving b0 19% short at an edge and 30% at a corner — conservative, but not the clause, and invisible for a square column because the two errors cancel until c1 ≠ c2. Both are asserted a second way — the interior perimeter less one whole side less d/2 off each survivor (2600 → 1800 → 1150 for c 500, d 150) — so the formula and the derivation have to agree, plus a rectangular-column case proving orientation is not interchangeable, which is where the transposition hid. Interior is proven byte-identical to the expression it replaced over a 120-case sweep, so nothing centred moves. The pad now carries the geometry the selector claims: `columnOffset` puts the column face flush with each free edge, giving e = (B − c)/2 from the pad centroid, the trapezoidal service pressure that follows, the kern check, and the strap moment P·e that would fix it — and asserts the fact that makes property-line footings what they are, that the eccentricity GROWS with B, so a pad cannot be enlarged out of the problem (proven on a small and a large pad, both failing the kern). The page's verdict now owns it: it read "DESIGN OK — all checks pass" beside a drawing showing uplift, and now fails on the kern with the reason and the remedy stated. Drawing rendered in headless Chromium over CDP in all three positions: the column at its edge or corner, the free edges marked, the critical section drawn open on them, and the pressure trapezoid on its own datum clear of the slab — it was originally drawn with zero ON the base, so the uplift half climbed back up through the footing it was describing |
| Rectangular flexural capacity — analysis, not design's inverse | `flexure.test.ts`, `scwb.test.ts`, `beamSolution.test.ts` | `rectCapacity` is the one place a singly-reinforced rectangle turns steel into a moment, added after the T-beam fix to answer the same question everywhere it is asked. Design and analysis are NOT inverses: the design routes solve As from Mu with ρ capped at the tension-controlled limit, so As·fy is exact there by construction, while analysis takes As as given and nothing stops it being over-reinforced. An audit of every flexural site found two carrying the yield assumption — `scwb.concreteBeamMn` (feeding ΣMnb in §418.7.3.2 and the §418.6.3.2 ratios) and `lib/beamSolution.beamProvidedCapacities` (the beam page's reported φMn, which also hard-coded φ = 0.90). Both are REACHABLE, measured rather than assumed: on a sweep of 9765 designs the pipeline's own predicate accepts, 68 (0.7%) sit below yield, the worst a 200×300 in f'c 21 with fy 550 and ⌀32 bars, where §9.6.1.2's two-bar minimum alone puts ρ at 0.034 — reported 87.7 kN·m at φ 0.90 against a true 51.7 at φ 0.65, **69% overstated**; and on a heavily loaded SMF frame the cut sections handed to `concreteBeamMn` come back at fs/fy = 0.75. Downstream effect measured before and after on real frames: SCWB min ratio 0.285 → 0.293 (Mnb is the DENOMINATOR, so overstating the beams was making joints look worse than they are — no pass/fail flipped), moment-ratio checks unchanged because both faces scale together. Verified three ways: equilibrium closes (0.85f'c·b·a = As·fs) and fs sits on the strain diagram at the reported c, over a 405-case sweep in which c is proven strictly inside d so the steel never goes compressive; φ is shown to follow εt at the EXTREME layer, with dt > d raising φ and leaving Mn untouched; and the whole solve is cross-checked against `tBeamCapacity` given a flange the block never leaves — two independent implementations of the same equilibrium agreeing to 1e-9. Audited and cleared: `columnDesign` and `scwb.concreteColumnMn` already clamp fs to ±fy by strain compatibility, `prestressedBeam` uses fps per §20.3.2.3.1, and every standalone engine (footings, pile cap, slab DDM, stair, wood-Armer, retaining wall) is design-side only. `pushoverModel.plasticMoment` now goes through it too (`pushoverModel.test.ts`): it used ρbd²fy(1 − 0.59ρfy/f'c) on a caller-supplied ρ, which is As·fy·(d − a/2) written out and so carried the same assumption, on a value the Model Space slider let the user set freely. That closed form does not merely overstate — it is a downward parabola in ρ: it peaks, turns over around ρ ≈ 5%, and is NEGATIVE above ρ ≈ 6.5% at f'c 21 / fy 550, handing a negative plastic moment to the hinges. Asserted both ways (old negative at ρ = 0.065, new positive; new monotone in ρ over 2–12% and flattening toward the concrete-crushing limit rather than turning over). Where the steel does yield the two agree to 0.05% — the textbook 0.59 against the exact 1/1.7 = 0.588235 — and the test now pins the exact value with the rounded one asserted as 0.99955 of it, rather than pinning the rounding as the answer. Measured across the range: identical to 0.1% at the 1.5% default, overstated by up to 24.4% at ρ = 3–4% with fy 550. The two ρ inputs are bounded to 0.1–5% (below §9.6.1.2's minimum a hinge is a fiction; the top is already past §418.6.3.1's 2.5% cap) through a new `min`/`max` on `Num` that clamps the VALUE, since the HTML attributes alone are advisory and a typed or pasted number passes straight through them — `clampTo` unit-tested in `lib/clamp.test.ts`, the wiring asserted against the page source, and the field driven in a browser to confirm 40 → 5, −9 → 0.1 and 2.2 unchanged |
| T-beam flexure — the steel that does not yield | `tbeam.test.ts`, `tbeamSolution.test.ts`, `validation.ts` `tbeam-no-yield` | `tBeamCapacity` took **T = As·fy** unconditionally and stopped there, which solves a different equation from the one equilibrium poses whenever the section is over-reinforced. Real equilibrium is C(c) = T(c) with C rising in c and T = As·min(fy, Es·εcu(d−c)/c) falling, so there is one root; assuming yield returns a neutral axis too deep and an Mn too large. Caught against a worked NSCP 2015 lecture (bf 800 · hf 100 · bw 300 · d 435, f'c 28, fy 345) which carries TWO steel areas on one section, deliberately either side of yield: at As = 6000 the steel yields, φ = 0.90, Mu = 708.048 kN·m — which the engine already matched to the digit, so the geometry, the two-couple Mn and the φ ladder were never in doubt; at As = 9000 it does not, φ = 0.65, Mu = 648.999, and the engine returned **672.338 — 3.6% UNCONSERVATIVE**, its a = 268.207 / c = 315.538 being exactly the trial the lecture writes down and crosses out. Fixed by solving the quadratic the substitution leaves (0.85f'c·β1·bw·c² + [0.85f'c·hf(bf−bw) + Es·εcu·As]·c − Es·εcu·As·d = 0 in the web branch, the flange branch when β1c ≤ hf), exact in one step; both branches have A > 0 and D > 0 so the positive root always exists and is proven < d, i.e. the steel is always in tension. Every intermediate is asserted against the board's own printed value, not back-computed: Aconc 122113.354, a 240.378, c 282.797, fs 322.922, ȳ 91.450, Mn 998.460 — and the discarded trial (268.207 / 315.538 / 227.159) is asserted too, so the correction cannot silently stop happening. The pair is also asserted as a pair: **more steel, less design strength**, because φ falls further than Mn rises. Mn is cross-checked two ways, as two couples and as T(d − ȳ), agreeing to 1e-13. `validation.ts` re-derives it by BISECTION on the residual — a different method with no convergence question (a fixed-point iteration on c = a(T(c))/β1 was tried first and orbits, so it was discarded rather than tuned) — agreeing to 15 digits. The pre-existing unit test that asserted `C = As·fy` was itself pinning the bug and now asserts `C = As·fs`, with a companion proving the sweep straddles yield rather than sitting on one side of it. The worked solution prints the check and the correction instead of only the result, and the page gained the analysis mode that makes the case reachable at all — it was design-only, so the one situation where the steel may not reach fy could not be entered. Rendered in headless Chromium over CDP with the lecture's numbers typed into the real inputs. `dGiven` lets the effective depth be STATED rather than derived from h and the cover — a textbook gives d, a drawing gives h — on this engine and on `beamDesign`; it pins dt with d (the single-layer reading, conservative because a smaller dt lowers εt and so φ) and stops the layout↔depth iteration, so the published pair is reproducible verbatim through the page. The real-T-beam determination is reported as a check that RESOLVED, in the code's own terms (Aconc = As·fy/0.85f'c against bf·hf — 130462.18 > 80000 for the worked case, the board's own figures): both geometries are valid outcomes, and marking "true T" as a red result made an ordinary flanged beam read as a failed one |
| Flanged beams in the model-space pipeline (T and L) | `beamDesign.test.ts`, `pipeline.test.ts`, `modelReport.test.ts` | The pipeline described a flange by SUBSTITUTING b_f for b and keeping that result only while the stress block stayed inside the slab; past that it fell back to the bare WEB rectangle — a real section, but the wrong one, since the overhangs are still there carrying full stress over h_f. `designBeam` now takes the flange as a description (`flange: {bf, hf, kind}`, `b` staying the web) and picks the branch itself: a rectangle of width b_f while a ≤ h_f, and the §406.3.2 two-couple TRUE T past it — A_sf = 0.85f′c(b_f−b_w)h_f/f_y at a fixed lever arm d − h_f/2, with the web rectangle carrying M_u − φM_uf, in SRRB and DRRB alike (the residual the compression steel sizes against is already net of the overhang couple). Agreement with the standalone `designTBeam` is exact — same A_s to the digit on every case where that engine returns a valid design; the cases where the two part are the ones it declines (`ok=false`, web remainder past the singly-reinforced ceiling) and `designBeam` carries on doubly reinforced. The adoption gate was measured, not assumed: on a 2584-case sweep where BOTH the flanged and the web-rectangle designs are valid, gating on tension steel alone would have REJECTED the T in 4.6% of them, because past ρ_max the flange couple's lever arm (d − h_f/2) can be shorter than the compression couple's (d − d′), so a true T wants marginally more tension bars while shedding the whole compression cage. Gating on TOTAL steel instead: the web rectangle costs 32% more on average, up to 117%, and never less. ρ_min stays a WEB property throughout and is asserted not to scale with b_f. `memberFlange` names the shape from the slab panels that frame in — two panels → a symmetric T on Table 406.3.2.1's interior row, one → an L (spandrel) on the edge row, one overhang — and `flangeKind` carries it to the schedule, the section drawing, the PDF caption and the report figure, where the L's web now sits flush with its flange instead of centred under it. The block depth is reported by the engine (`a`, `aReq`, `AsProv`) rather than re-derived in the component as a b_f-wide rectangle a true T does not have; equilibrium is asserted both ways (SRRB: 0.85f′c·b_w·a = (A_s − A_sf)f_y; DRRB: A_s·f_y = C_flange + C_web + C_s′). Rendered in headless Chromium: the flange-action, true-T and true-L sections drawn from the real engine output, the stepped block confirmed in the SVG (flange band at full b_f down to h_f, then a web strip of width b_w down to a = 148 > h_f = 120), and an overprint fixed on the way — the long `a = 114  (req 101)` callout ran past the overhang and crossed the web outline, so the parenthetical is now dropped whenever the separate a,req rule is drawn (threshold lowered so it usually is) or the text will not fit the overhang. End to end through `designStructure`: with T-beam action on, every sagging section of a two-bay grid is flanged (T on the interior lines, L on the perimeter), and on a 75 mm slab under 80/60 kPa the true-T branch reaches the schedule with A_sf matching 0.85f′c(b_f−b_w)h_f/f_y to 1e-6. |
| Stair kink detailing, and the slab cage the 3D view did not have | `stairLayout.test.ts`, `slabCage.test.ts`, `cageBuilder.test.ts` | **The stair.** The flight elevation ran both bar layers landing-to-landing as one bent bar and added a "lapping bar" at all four corners — itself bent round the same re-entrant corner it was added to relieve. At a bend the tension either side resolves along the bisector, towards the OUTSIDE of the turn: where that is concrete the bar bears on it, where it is the cover the resultant drives the cover off. On a flight rising to the right both layers turn UP at the bottom kink, so the soffit bar (whole waist above it) is bent and the top pair cross instead; the top kink is the mirror. Two bends in the flight where there were four. A crossed bar cannot simply run straight either — 450 mm out of a landing's top face leaves a 150 waist through the soffit after about 240 mm, which the first attempt drew, bar hanging outside the concrete — so it turns at the far face and spends the rest of its anchorage along it, developed length past the corner always the stated 450 (asserted over ext = 40…900). The turned leg is placed a bar clear inside the layer it laps, because laid on that face it is hidden underneath it. `barPath` gained per-end hooks (a crossed bar has one free end and one buried) and names its hook direction by where it goes on the PAGE — the old signed perpendicular flipped with the run direction, hooking the same soffit bar up when drawn left-to-right and down when drawn right-to-left, out through the cover. Drafting: eight anchorage dimensions stacked round two corners became one per kink following its own bar's shape; spacing callouts moved onto arrowhead leaders, one per zone, instead of text laid over the member; R and G are dimensioned on a step; θ moved below its own leg. Six geometries rendered in headless Chromium and every label checked against every other label and against the linework beneath it — zero overlaps, zero outside the frame, zero on a line. Two defects were found that way and not by eye: the flight callout rotated along the slope across the step outline, and θ printing over the waist callout on a short flight. `offsetPath`'s docstring claimed a parallel offset preserves length; it does not (round the inside of a corner it is shorter), the test proved it, and the claim was corrected rather than the test relaxed. **The slab cage.** The 3D cage had beams, columns and footings and no slab steel at all — the one element covering the whole floor was the one drawn bare. `slabCage` builds both mats from the panel's own DDM strips (the spacing the design adopted, per strip, per direction) and the extensions already encoded in `slabBarDetail` from ACI Fig. 8.7.4.1.3 / NSCP Fig. 408.7.4.1.3: bottom mat continuous with 150 mm into each support, top mat over the supports only — 0.30/0.20 ℓn column strip, 0.22 ℓn middle — asserted to leave mid-span bare, which is the whole point of the cut-off. Short span in the outer layer of both mats, layers a bar apart, every bar inside the slab. A continuous edge stops the top bar at the support CENTRELINE so the neighbour draws the other half (asserted: no top bar crosses an interior support, where drawing both sides gave two mats); a free edge turns it down into the support, the hook clamped to the depth between the mats and a note raised when §425.3.1's ℓext will not fit — a 200 mm slab leaves 140 mm for a ⌀10 hook that wants 150. Chairs are the standing Z the reference photo shows, omitted with a note when the slab is too thin to stand one. Verified by projecting the placed cages to plan, section and isometric and reading the drawings; the take-off is untouched and still measures slabs from the DDM strip areas, asserted so the bill cannot count them twice. |
| Two-way slab: bent-bar (cranked) main steel | `slabCage.test.ts` | Rebuilt against a reference detailing animation, which shows the arrangement the first cage did not have: every main bar is CRANKED — bottom through midspan, bent up near a support, finishing as top steel over it — with alternate bars cranking at opposite ends, so each support receives half the bottom bars as top steel while midspan keeps all of them. That factor of two is the whole arithmetic of the detail and is where `extraTopSpacing` comes from: areas add, so the straight top bars that make up the shortfall are spaced at 1/s = 1/s_top − 1/(2·s_bot), and there are NONE when the cranked half already carries it (asserted both ways, and asserted to reconstruct s_top exactly when combined). The cranked bar's top leg reaches the same 0.30 ℓn past the face that a straight top bar would, so the two details put steel over the same length of support and can be compared rather than merely swapped. The crank climbs between the two layers at a stated angle (45° by default, asserted to shorten its run when steepened). The old straight-mat arrangement is kept as `detail: 'straight'` — both are real, they are not the same bar schedule, and the choice is stated rather than assumed; both are asserted to place the same bottom mat at midspan, since the details differ in how the top steel arrives and not in the span's own design. One bug found by reading the drawn coordinates rather than the tests: the far end of a cranked bar was written in signed form as `farFace - farSign * -embed`, whose double negative pulled the bar 150 mm SHORT of the far support instead of 150 mm into it. Now written out, and pinned. Verified by projecting the placed cage to section and isometric: the section shows one bar top-over-the-support, cranking down, running the bottom through midspan and rising at the far support, with the straight top bars and the chairs around it; the isometric shows the cranks stepping up over every support line, as the reference does. |
| Slab cage vs the beam it sits on | `slabCage.test.ts`, `cageBuilder.test.ts` | Reported from the 3D view: slab bars drawn OUTSIDE the beam, on the side and over the top. Two separate defects. **Sideways.** A panel's edge is the supporting beam's CENTRELINE, so the concrete a bottom bar has beyond that edge is HALF the beam's width — and the cage carried §408.7.4.1.3's 150 mm past it unconditionally. On a 250 mm beam that ends the bar 25 mm outside the concrete with no cover at all; on the module's own 300 mm default it lands exactly on the far face, which is zero cover and equally undrawable. The embedment is now clamped to `s/2 − cover` per edge, which is the reading `slabBarDetail` already took for the elevation (`Math.min(embed, face)`) — the two had simply disagreed. The width comes per edge from the beams that really frame it (`edgeSupport`, narrowest wins) instead of `model.sections[0]` for all four, because one width for four edges has no reason to be the width of the beam the bar actually ends in. A support too narrow for the full embedment now raises a NOTE ("bottom bars carry 105 mm into a 250 mm support…") rather than being detailed short in silence — the anchorage really is less than the figure asks for, and that is a finding. Asserted: no vertex of any run, in either detail, past the edge by more than that edge's own half-width less cover; a 500 mm support still takes the full 150 mm and raises nothing. **Over the top.** Not the cage — the SOLID. `Slab3D` drew the panel from the node level UPWARD at a hardcoded 100 mm, but a floor's node line is the top of the beams framing into it (`levelDrop`), so the panel stood a whole thickness proud of its own beams and the correctly-placed cage hung below the concrete it belongs to. The box now hangs BELOW the node at the plate's own thickness, which puts the slab's top face and the beam's top face on one line, as a floor is. **And the bars are now lapped.** Slab mats were the one cage `cageBuilder` did not run through `spliceCage`: a 6 m panel plus two embedments is 6.26 m of bar and a stock bar is 6 m, so every bottom bar in a normal panel was drawn as one piece nobody can buy. Bottom laps in the end eighths, top at midspan, staggered per §25.5.2 — the arrangement the beams already used. |
| Slab steel against beam steel | `slabCage.test.ts`, `cageBuilder.test.ts` | Reported from the 3D view against two site photographs. **The top mat was placed from the slab's own cover and nothing else**, which put it ABOVE the beams' top bars — 20 mm of slab cover is less than a beam's 40 mm plus its 10 mm stirrup, so the mat floated 18 mm clear of the steel it is physically laid on. A monolithic floor is one pour and the beam's top bars are the highest steel in the section; the mat rests on them. It now does, by one `Math.min` that reads correctly both ways: resting is normally the lower of the two levels and governs, and a beam bar high enough to break the slab's own cover simply stops governing, so neither is a special case. **The bottom bar ran straight into the support and stopped.** At an edge where the slab carries on that is right — the panel next door draws the same bar through. At an edge where it STOPS, a bottom bar developed by nothing but bond over one beam width is the anchorage §408.7.4.2 asks for and does not get; the site photograph shows it turned up and hooked over the beam's own top bar. It now turns up there and only there, the leg reaching that bar, raised toward the top mat where that would be shorter than §425.3.1's ℓext and never taken past it into the cover. A leg the slab depth cannot give ℓext to raises a NOTE naming the length it managed ("bottom bars hook up 68 mm onto the beam's top bar…") rather than being drawn short in silence — on the frame this ships with, a 150 mm slab gives 68–80 mm against ⌀12's 150. The level itself is derived, not assumed: a beam's node line IS its top face (`levelDrop`) and `beamCage` insets its top bar by cover + stirrup + half a diameter, so the top of that bar is cover + stirrup below the node. `cageBuilder` takes the HIGHEST across the four framing edges, because the mat is one plane laid across all of them. Asserted: the mat's outer layer sits one bar radius above that level and the inner layer a diameter under it; cover wins when a beam bar is flush with the slab's top face; a discontinuous edge gets a vertical leg reaching at least the beam bar, a continuous one gets none, and the far end of a CRANKED bar — bottom steel arriving at a free edge like any other — gets the same hook. **Both fall back** to the previous cover-derived levels and straight embedment when no beam level is supplied, so a caller with nothing to measure still gets a cage; asserted too, because that is the path every existing test takes. |
| The cage drew steel the design did not ask for | `cageBuilder.test.ts`, `slabCage.test.ts` | Three findings from one session with the 3D view, all of them the cage disagreeing with its own worked solution. **189 stirrups on the beam with the least shear.** §409.6.3.1 excuses a beam from shear reinforcement at Vu ≤ φVc/2 and `beamDesign` says so honestly — region "none", `sAdopt` 0, `sHinge` 0. `cageBuilder` filtered those zeros out, found nothing left, and passed 0 down; `stirrupStations` clamped it to the 25 mm floor it keeps against a degenerate input and laid the WHOLE span out at it. Reported as "the analysis says there is little shear here but the stirrups say otherwise", and it was also a BILLING defect: `takeoff` counts stirrups off the placed cage, so the same beams were over-billed about five-fold in steel and tie wire. The detailing decision now lives where the section is known — `cageBuilder` supplies the §409.7.6.2.2 nominal, d/2 capped at 600 — and the floor in `stirrupStations` falls back to something buildable rather than to itself. Asserted on the reported scenario exactly (a loaded floor with one panel deleted): the affected beams come out at 220 mm, under 60 hoops instead of 189, and NO beam anywhere in the frame is laid out at the floor. **Extra top bars on their own grid.** In the bent arrangement the cranked bars deliver top steel at twice the bottom pitch and straight bars make up the shortfall; those were placed by `bandLines` at the extra spacing, an even grid of their own, independent of the cranked bars sitting at the same level. On the sample panel the first straight bar landed 21 mm from a cranked one and drifted from there. They now go on the bottom mat's own setting-out grid, in the gaps the cranks left — at one support the free positions are exactly the bars that cranked at the FAR end, which is how a bar schedule is really set out. `everyOther` is the pure helper and is asserted directly: it never returns a position a cranked bar occupies, it spreads a smaller count rather than crowding one end, and it stops at the gaps it has when asked for more, which is a real limit of the detail rather than a licence to double bars up. **Chairs under nothing.** The top mat exists only in bands over the supports — the cut-off is the point of the figure and a test already asserted midspan is bare — but the chairs were laid on a grid across the whole panel, so the middle of every panel carried standing bars holding up air. A chair is not a spacer; it holds the top mat at the level the negative-moment capacity was designed at. They are now placed only where a top band actually passes, in EITHER direction, since a point at midspan along one axis can still sit inside the other axis's band; asserted all three ways. |
| Stair as a model entity — the geometry the frame implies | `stairPlacement.test.ts` | A flight names two members and a riser count; rise, run, R, G, θ and the four corners of the waist all follow from where those members are. DERIVED rather than declared, because equal risers are not a preference: with R, G and the two support levels all stated independently, three of them fix the fourth and a model can hold a flight whose treads never reach the landing. Deriving R = rise/risers and G = run/risers closes the geometry by construction — asserted both ways on a frame whose numbers check in the head (two beams 3 m apart in plan and 1.5 m in level, 10 risers → exactly 150/300) — and turns validation from "do these four numbers agree?" into the question worth asking: is the stair this frame implies a usable one? Bearing is the TOP of each member, h/2 above the node, the same convention `cageBuilder` places a soffit by. The flight is set out square to the LOW support and pointed at the high one (asserted to reverse when the high support moves to the other side), so supports more than 5° out of parallel are an error: the run would miss. Placement REFUSES three things and says which — no climb, the two named the wrong way round, and a support that is vertical in plan — while a merely uncomfortable stair places and warns, which is the split that matters (a broken model must stop the run, an unusual stair must not). The usability ranges and 2R + G are labelled rules of thumb, not code clauses, and the test asserts that wording. **The load path.** The flight is not meshed: it is designed by `engine/stair` and its reactions go onto the two members it bears on, the route a `Wall` already takes. Spread across the bearing WIDTH as point loads at the centroids of equal sub-strips — one load at the strip centroid doubles the midspan moment of a flight covering its whole support, a full-length UDL smears a narrow flight over a beam that continues past it, and this needs no new load kind to avoid both. Total, split and line of action are all exact (asserted); the local moment converges to the partial UDL the model has no kind for. Generated inside `buildGravityLoads`, which drops every D and L load first, so a flight that moves cannot leave a stale load behind. Proven end to end on a two-storey frame: 16 point loads, ΣD 44.99 kN and ΣL 28.80 kN matching q_dead·(run × width) and q_live·(run × width) to the digit, and the service footing reactions rising 749.5 → 823.3 kN — exactly the flight's own weight, arriving at the foundations. **Stated limit:** an unmeshed flight adds no stiffness. That is conservative for the frame and NOT conservative for the stair, which in reality is an inclined strut that attracts storey shear and can shorten the columns it frames into. Meshing it is a separate phase. |
| Stair cage — the steel in a modelled flight | `stairCage.test.ts`, `cageBuilder.test.ts` | A flight is a one-way slab spanning up the slope, so its cage is a slab strip's: main bars up the slope in the tension face, distribution bars across them, and an anchorage into each of the two beams it bears on. Built from the same `designStair` row the schedule prints, placed by the same `placeStair` the loads were built on, so the bars drawn are the bars designed. Cover is measured NORMAL to the soffit — asserted against the flight's own normal, because measured vertically it is t/cosθ on a slope and the bar sits proud of the face it is meant to be buried under. Distribution steel is asserted to sit INSIDE the main bars, which is what leaves the main bars the larger d. Which ends carry top steel comes from the end condition rather than a flag: simple gets none (and says so — any restraint the beams really provide is then uncracked concrete), `one-end` gets it at the TOP where the flight runs on into the floor, `both-ends` at both; the top run is a quarter of the span from the face, left as a parameter because it is a detailing choice the drawing prints. Every bar asserted inside the flight in all three directions, and a waist too thin to hold two covers, two main bars and two distribution bars raises a note. **No kinks here yet.** `StairElevation` draws a flight WITH its landings, so it has two re-entrant corners and needs the crossed-bar rule. A `Stair` was a bare flight between two beams when this shipped, so there was no kink in the geometry and nothing to cross; half-landings have since made that kink real, and the cage says in its notes that the landing is bare rather than drawing bars that quietly stop where the slope does. Stated in the module either way, rather than left to be discovered. Two test-side errors found and fixed rather than papered over: depth was being read at a main bar's HOOK TIP (which turns up into the beam and so sits nearer the top face than the bar it anchors), and the across-width bound was measured from a flight CORNER while being compared against ±half a width. |
| Lintel beam — the load, which is what makes it its own calculation | `lintel.test.ts` | The design is an ordinary RC beam; the lintel-specific part is how much wall reaches it. Masonry over an opening ARCHES — a triangle bears on the lintel and the rest is carried round to the jambs — and the triangle is taken as equilateral, apex 0.866·ℓ up. The angle is an INPUT because it is an assumption, not a clause, and a shallower arch is a heavier lintel (asserted to move both ways). **The arch has to have room to form.** Where the wall above is shorter than the triangle, there is no arch and the whole rectangle of wall bears — the case that catches people out, a lintel just under a slab soffit, and asserted to be the heavier one. Decided from the geometry rather than left to be remembered, and reported in a note. A load applied above the apex is carried round to the jambs; it is reported as excluded rather than silently dropped, and the same load with no arch to carry it lands on the lintel (both asserted). **The triangle is a TRIANGULAR load.** Smeared to a uniform load of the same total it gives M = Wℓ/8; peaking at midspan it gives Wℓ/6, 33% more — asserted against the closed form and against the smeared value it replaces. Effective span is ACI §6.3.2.1 (clear span + h, never more than the support centres — both branches asserted) and bearing is §22.8.3.2 φ(0.85f′c) over the contact area. Two test premises of mine were wrong and were corrected rather than the code: a 10 mm bearing under a 2 m opening still PASSES (8.4 MPa against an 11.6 limit), so the failing case had to be a genuinely overloaded one; and a 200 × 300 lintel over 2 m and over 3.2 m has the same steel because ρ_min governs both — the honest answer, so the test now asserts the minimum first and reads equal steel as correct rather than as a broken engine. The elevation draws the assumption, since it is invisible in a table: the triangle, or the whole rectangle with the reason, the bearing dimensioned on the overlap it is checked over, and a break line where the wall carries on above. Rendered over three cases in headless Chromium and checked label-against-label and label-against-linework. The checker itself was corrected in the process — it had been flagging text over a shape's FILL as a collision, which a label inside a tinted box is not. |
| Stair span — the plan run, and the drawing that agreed with it | `stairLayout.test.ts`, `stair.test.ts`, `stairPlacement.test.ts` | The stair page fed ONE number to two engines that meant different things by it. `designStair` computes Mu = w·L²/denom with w in kPa of PLAN area, so its span is the horizontal projection; `flightGeometry` documented the same input as the length along the SLOPE and projected it again (span·cosθ). The drawing that came out matched neither: at span 3.5 with R 150 / G 300 it drew a 3.000 m run and a 3.354 m slope and labelled its HORIZONTAL span dimension "3.50 m along the slope" — 14.3% off as a plan run, 4.2% off as a slope length — while `designStair` was handed the same 3.5 and returned a moment **36% higher** than the flight beside it had. Fixed at the layer, not the label: the drawing is set out on the plan run, and the span dimension now prints what was DRAWN (run, risers and the derived slope) rather than the input, because rounding to whole steps moves the geometry and a dimension whose number came from the input while its line came from the drawing is a dimension that lies. The page's field says (plan). Asserted three ways: the step count is the run the input buys and not that run projected again; the drawn run, rise and slope are one triangle and within half a going of the input; and the drawn run is the flight's actual horizontal extent on the page. `designStair.ok` separately ignored `tMinOK` — the Table 407.3.1.1 ℓ/20…ℓ/28 one-way-slab minimum it computes on the line above — so a 150 waist over a 4 m span returned ok:true with tMinOK:false printed next to it. A reader on the page could see the contradiction; nothing reading `ok` could, and the model-space schedule row would have inherited it. Folded into the verdict, the way the RC beam path already folds its own `thickOK`. **The flight in 3D.** `flightSolid` builds the waist prism and the treads outside the React component so they can be checked without a renderer, and that is what caught the bug an isometric only hinted at: treads were positioned by a CORNER of the flight while a renderer centres a box on its origin, so every step was drawn half off the side of the waist. Now asserted the definitive way — each tread's four base corners transformed into the flight's own run/width coordinates and required to land inside the waist's top face — plus the waist normal proven perpendicular to both the soffit and the width, its vertical thickness equal to t·cosθ, and the last tread arriving exactly at the upper bearing level. End to end through `designStructure`: the schedule row's Mu matches wu·run²/8 to the digit on the plan run, and the flight's reactions (D 44.99 / L 28.80 kN) are the ones on the frame. |
| Stair half-landing, and the beam it breaks on | `stairPlacement.test.ts`, `stair.test.ts` | A stair between floors is two flights meeting on a beam at mid height, and the flight that carries the half-landing was not something the model could hold: a `Stair` ran beam to beam with nothing flat at either end. A landing is now part of the SAME one-way slab, not a second span — which is the modelling decision the rest follows from. It eats into the run before R and G are struck, so the flight climbs the same rise over less of it: on the fixture (supports 4.2 m apart in plan, 1.5 m in level, a 1.2 m landing, 10 risers) the going is 300 mm off the 3.0 m slope and not the 420 mm that dividing the whole 4.2 m run would have given — a going nobody builds, off a stair that is actually fine. The design span does NOT change with a landing and the test says why: `designStair` puts kPa of PLAN area through w·L²/denom, so the length that belongs with it is the horizontal distance between the supports, landing included. What changes is the LOAD, and only downwards — `landingLoads` is the flat-slab case with the two terms that make a flight a flight taken out (no 1/cosθ on a vertical thickness, no γc·R/2 of treads), asserted lighter than the flight at every slope tried and asserted to MEET `stairLoads` exactly at R = 0, where the two formulae describe the same slab. So designing a stair with a landing on the flight's own load over the whole span is conservative rather than an error to correct, and that is stated on the schedule row rather than left to be inferred. **The reactions are not approximated that way.** The old split was half to each support, which is symmetry and not an assumption about continuity — but only while the load is uniform. With a landing it is not: the lighter strip sits at one end, so the split comes from ΣM about the low support over the three strips, asserted against the hand value (a 1.2 m landing at the low end sends 18.00 kN of the 33.71 kN dead to the HIGH beam, 6.8% more than halving would have) while the live load, uniform over the whole plan, still halves. The total is exact either way, and the total is what must not be lost. Geometry: the landing is flat at its bearing level, its thickness measured VERTICALLY (asserted, against the waist beside it still measured normal to its own soffit), treads laid over the sloping part only and starting at the landing's inner edge, and `bearLow`/`bearHigh` separated from `lowEdge`/`highEdge` so the reaction lands where the slab bears rather than where the slope starts. Validation splits the same way the rest of the stair rules do: landings that leave nothing to slope, two at one end, or one with no depth are errors that name the landing rather than blaming the supports for it; a landing shallower than the stair is wide, or thinner than the waist it continues, is a warning. **Stated gap:** `stairCage` reinforces the sloping part only and now says so in its notes — a landing makes the re-entrant kink real in 3D, and that detail is `stairLayout`'s crossed-bar rule, to be lifted into one shared module so the two drawings cannot disagree. |
| Stair kink rule, shared by the two drawings that need it | `stairBarDetail.test.ts`, `stairCage.test.ts`, `stairLayout.test.ts` | The crossed-bar detail at a re-entrant kink lived in `components/stairLayout`, where only the 2D elevation could reach it, and the 3D cage said in its own header that this was fine because a modelled flight had no kinks. Half-landings made that false. The rule now lives in `engine/stairBarDetail` — planar, unit-agnostic, y DOWN — and BOTH drawings ask it: the elevation in SVG units, the cage in metres in the flight's own vertical plane, so the same corner cannot be detailed two ways. **Generalised from two kinks to one.** A flight breaking on an intermediate beam has a landing at ONE end, and the old four-point form fell through to two straight bars for it — the kink silently undetailed. Which layer crosses is now decided per kink from the geometry (`kinkKind` reads a flat leg as a landing), so a lone bottom kink gives one continuous soffit bar and a crossing top pair, a lone top kink the mirror, and the four-point case is unchanged (its ids, bends and anchorages all still asserted). The piece carrying the FLIGHT is the `-through` bar and the piece living on a landing the `-lap`, which reproduces the existing names without being a special case. **In model space.** The cage sets the plane out from the low BEARING edge and builds each bar line as the intersection of the faces' own offsets — not the point under the face corner, which at a re-entrant kink is out by a·(1 − cosθ)/sinθ in the direction that leaves the concrete (58 mm on the test flight, asserted). Cover on the slope is normal to the soffit, on a landing vertical, because that is how each slab is built. Measured, not assumed: every bar vertex between the two bearings is inside the concrete, and the tightest NORMAL clearance over all three landing arrangements is 0.026 m to nine decimals — exactly 20 cover + ⌀12/2, never less. Comparing vertically instead would have passed a bar at 26 mm measured the wrong way, so the check divides out cosθ. Exactly one bottom and one top bar per across-width line crosses the flight, so the four bars per line are two bars cut and lapped rather than four running side by side. Both layers run the whole developed length where there is a landing — a re-entrant kink puts the top face in hog whatever the bearings do, so the end condition has nothing to say about the top steel there — while a bare flight keeps the plain arrangement it had (bottom throughout, top only over an end the design calls continuous), asserted unchanged. One test-side error found and fixed: `byId('top-through').pts[1]` was reading a position in an array rather than the anchorage, so when the refactor reversed which end that bar is written from, the assertion kept passing on the wrong point; it now reads `anchor.run[0]`, the point where the bar actually met the far face. |
| Half-landings in the model space | `pages/stairLandingUi.test.ts` | The panel gained a landing depth per end, the 3D view draws the landings as part of the same solid (one prism call per slab, not a flight with something stuck to it), and both the panel list and the report schedule carry a landing column. Two wiring facts are pinned because they are where a landing would quietly go missing: the landings are built in ONE place and BOTH the add and the live R/G/θ preview use it (computed without them, the preview advertises a flight nobody is about to add — the going would read 420 mm where the stair is 300), and the column is asserted present in the table HEADER and the ROW together, since adding it to one alone shifts every later cell under the wrong heading. A landing that leaves nothing to slope now names the landing: the panel places the same stair WITHOUT its landings to tell that apart from supports that genuinely cannot carry a flight, which `placeStair` reports identically. **Stated verification limit:** this repo has no DOM testing library (see `ErrorBoundary.test.tsx`) and `/model` sits behind `RequireAuth`, whose Supabase host this environment's egress proxy blocks — so the page was NOT driven in a browser for this change, and the wiring is asserted against the source the way `lib/clamp.test.ts` already does. The geometry behind it is not asserted that way: `flightSolid`'s landing prisms are unit-tested in `stairPlacement.test.ts` (flat, thickness measured vertically, corners in run order, treads over the sloping part only). |
| Standalone calculators | `retainingWall.test.ts`, `stair.test.ts`, `waterTank.test.ts`, `pileCap.test.ts`, `rectangularFooting.test.ts`, `flexibleCombinedFooting.test.ts` | textbook worked examples per the L7 pattern (each page prints the step-by-step) |
| Geotech | `geotech.test.ts`, `bearing.test.ts`, `soilNail.test.ts`, `micropile.test.ts`, `shotcreteFacing.test.ts`, `rockAnchor.test.ts` | Rankine/Terzaghi–Meyerhof factors vs published tables; FHWA GEC-7 / NHI-05-039 procedures (review anchors) |
| Soils — classification & index | `atterberg.test.ts`, `sieve.test.ts`, `uscs.test.ts`, `aashto.test.ts`, `classifySample.test.ts`, `soilFamily.test.ts` | ASTM D4318 flow index / one-point LL, D6913 gradation + Cu/Cc, D2487 group symbols against the plasticity chart and the dual-symbol rules, AASHTO M 145 group index evaluated SIGNED before flooring (40% fines at LL 20 / PI 2 is 0, not 1) |
| Soils — SPT & stress | `spt.test.ts`, `overburden.test.ts`, `sptProfile.test.ts` | D1586 N₆₀ = N·(E_R/60)·C_B·C_S·C_R with Skempton/NCEER factors, C_N = √(Pa/σ′v0) capped at 1.7 (Liao & Whitman), layered σ′v0 with a layer straddling the water table integrated in two parts |
| Soils — laboratory | `lab/lab.test.ts`, `lab/strength.test.ts`, `lab/consolidation.test.ts`, `lab/compaction.test.ts`, `lab/triaxial.test.ts`, `lab/permeability.test.ts`, `lab/hydrometer.test.ts`, `lab/swell.test.ts`, `lab/plots.test.ts` | D2216/D854 index tests and the test-registry plumbing; D3080 Mohr–Coulomb least-squares envelope; D2166 qu with cu = qu/2 refused outside saturated cohesive soil; D2435 e–log σ′ with Cc/Cr and σ′p recovered from breaks PLANTED at 100/200/400 kPa (the earlier Casagrande construction was wrong by 5× and passed a loose tolerance); chart geometry — log axes, round ticks, nothing drawn outside the frame (plates included); D698/D1557 compaction — ρd from the mould masses by hand calc, the peak recovered from a parabola PLANTED at w = 15% and again at 20% (and from points that straddle it without sitting on it), the zero-air-voids bound against its closed form and shown monotone, a point above it reported as impossible with the saturation it implies (a mould volume typed 844 for 944 is the planted error), and the fit REFUSED — highest measured point reported instead — when no peak lies inside the range tested; D2850/D4767/D7181 triaxial — the envelope is the COMMON TANGENT, verified by planting c and φ, building circles that touch that line exactly, and demanding the pair back to 8 dp, then measuring the distance from every centre to the fitted line and requiring it to equal the radius; cross-checked against σ1 = σ3·Nφ + 2c√Nφ; the Kf line is shown to be a DIFFERENT line (α = 30° is φ = 35.26°, a 5.3° error if read as tan φ); a saturated UU series returns φ = 0 with c = su and identical circle diameters, and a UU series that does not is reported as unsaturated rather than given a friction angle; CU total and effective envelopes both fitted, the effective circles shown shifted by u at constant diameter, and no effective envelope drawn from a partial set of pore pressures; sin φ = 1 refused outright and φ > 50° disbelieved in writing; the Mohr chart asserted to use ONE scale on both axes (a circle drawn on unequal axes is an ellipse and its tangency is a lie) and to mark the TANGENT point rather than the crown; the per-specimen FAILURE-PLANE stresses σf = p − r·sin φ, τf = r·cos φ asserted to lie on both the envelope and the circle, to carry exactly cos φ of the crown's τmax (so quoting τmax overstates the failure shear by 1/cos φ), to put the plane at θ = 45 + φ/2 with the arc subtending 2θ, and to degenerate to θ = 45° with τf = su on a φ = 0 UU test; D2434/D5084 permeability — k from both forms against hand calcs and against Darcy's own scaling (double the head, half the k), the D5084 viscosity ratio unity at 20 °C to within 0.5% and worth 30% across 10–30 °C, a constant-head test on a fine soil reported as measuring the apparatus rather than the soil and a falling-head test on a sand as unreadable, a rising head refused; D1883 CBR — the ORIGIN CORRECTION recovered from a planted 0.50 mm shift and shown to be worth the third of the value it hides (73.6% against 59.1%), both ordinates against the 6.9/10.3 MPa standard stresses, the 5.08 mm value reported with D1883 §11.3's re-run demanded in writing when it governs, and an extrapolated 5.08 mm ordinate flagged as one; D7928 hydrometer — Stokes' diameter shown to scale exactly as √(L/t), water viscosity 0.01002 poise at 20 °C from the Vogel equation, and the computed K for Gs 2.65 at 20 °C pinned at 0.01366 against the published table's 0.01344 so the 1.6% gap is a known quantity rather than a surprise; percent finer against a hand calc with a = 1.65Gs/(2.65(Gs−1)) unity at Gs 2.65, the MENISCUS-corrected reading used for the effective depth and the raw one for the mass; scaling onto the whole sample by the fines content asserted to leave the specimen-basis percentages untouched; and the four things the curve refuses to hide — a reading below the composite correction, more soil in suspension than was put in, a curve that climbs (sedimentation cannot), and a bath that drifted; D4546 swell/collapse — the strain kept SIGNED through to the headline (a 20.00 → 19.64 mm specimen reports −1.80% collapse, not 1.80% of something), the inundation stress carried with it because the same soil swells several times more at a seating load, the swelling pressure recovered from a planted log-interpolated crossing at 141.42 kPa and REFUSED when re-loading never returns the specimen to its original height, and method C's zero strain reported as zero-by-construction rather than as a measurement |
| Soils — liquefaction (NCEER) | `liquefaction.test.ts` | Youd et al. (2001): CSR vs a documented hand calc (0.3984 at 6 m, 0.40 g), CRR₇.₅ against the published clean-sand base curve at (N₁)₆₀ = 10 and 20 (0.113 / 0.215), MSF = 1.0 at M 7.5 by construction, K_σ capped at 1.0, fines α/β at FC 15% and the 35% saturation, Iwasaki LPI depth weighting; fines content resolved from the sieve record rather than defaulted; `validation.ts` `liq-csr`/`liq-crr`. Method boundaries are asserted as hard behaviour: no FS past (N₁)₆₀cs = 30, nothing evaluated above the water table or in cohesive soil |
| Soils — subsurface cross-sections | `section.test.ts` | correlation by IDENTITY (symbol then name) and never by position in the sequence — a test asserts the nth layer is not joined to the nth; lat/long projected to metres (0.001° ≈ 111.3 m) with an EVEN-SPACING fallback that says horizontal distances mean nothing; a stratum absent from the adjacent hole wedges to a point at the midpoint in BOTH directions rather than being carried across or leaving a void; the drawing asserts interpolated boundaries are dashed and hole traces solid |
| Soils — CPT (Robertson) | `cpt.test.ts`, `validate.test.ts` | q_t = q_c + u₂(1−a) hand calc, and the correction shown to be <1% in dense sand but >20% in soft clay — the reason an uncorrected "qc" is ambiguous exactly where it matters; normalisation ITERATED to a fixed point on the mutually-dependent n and I_c (sand n = 0.563, Q_tn = 106.8, I_c = 1.714; clay n pins at 1.0, I_c = 3.292), with non-convergence reported rather than the last iterate returned as truth; SBT zone boundaries continuous with no gap and never emitting a USCS symbol; Kulhawy & Mayne D_r and φ′ REFUSED in clay and s_u refused in sand, each with a reason; s_u shown to scale directly with the assumed N_kt; schema rules reject a reading below the sounding depth or a negative resistance, and warn when a cone sounding claims samples or SPT — a cone recovers nothing, so those belong to another hole |
| Soils — remote store & sync | `remoteStore.test.ts`, `supabaseRemote.test.ts`, `remoteDiagnostics.test.ts` | optimistic concurrency (a stale write is REFUSED with both versions attached); the four sync outcomes — uploaded / downloaded / in-sync / conflict — with a two-sided edit leaving BOTH sides untouched; one failing record does not stop the sync; change detected by CONTENT HASH, so an edit landing in the same millisecond as the last save is still uploaded; Supabase adapter against a postgrest-shaped fake: user scoping on every verb, a newer schema_version refused rather than parsed, one unreadable row skipped without failing the listing, transport errors surfaced instead of returning an empty list. The live-database self-test is itself tested: every one of its five checks is driven to BOTH outcomes, so a diagnostic that could never fail cannot ship |
| Structure report sheet set | `planSheets.test.ts` | the Plans tab and the PDF report build from ONE list (`buildSheetSet`), so neither can show a sheet the other lacks: every group populated from a model that has a slab opening and a shear wall, keys unique and filename-safe, three sheets per wall type in corner/intersection/joint order, each sheet's warnings its own (the corner sheet does not inherit the construction joint's), every sheet serialised to a real SVG *and* painted through `paintDrawing` with a recording jsPDF — nothing off the A4 page, no NaN, height capped to the column |
| PDF vector drawing painter | `drawingPdf.test.ts` | every `PlanPrimitive` mapped onto jsPDF vector calls with a recording fake: coordinates mapped into the target box, stroke widths and dash patterns scaled by the SAME factor as the geometry, fill/stroke/both paint styles, a primitive with neither skipped rather than painted black, dash pattern reset so it cannot leak into the next drawing, no NaN reaches any call, nothing drawn outside the box |
| Soils — report PDF | `soilsPdf.test.ts` | all 22 headings reach the rendered page (not just the model), not-assessed sections included; completeness page emitted BEFORE the first section; blocking integrity errors printed first; header says DATA CONSISTENT rather than "DESIGN OK", since a report checks no structure | laboratory figure captions reach the page and the omitted test's does not; the same investigation with and without its lab data is compared to show the plots take space rather than being silently skipped |
| Soils — report document model | `report.test.ts` | all 22 sections ALWAYS present and numbered, never dropped for lack of data; every non-complete section carries a stated gap; gaps collected onto the report; groundwater absence reported as "not a record of absence"; liquefaction distinguishes "evaluated and safe" from "nothing could be evaluated" (a real bug found by reading output, where the latter printed as an all-clear); provenance carried into the parameter table | §8 laboratory tabulates only tests that PRODUCED a result — a booked-but-unrun test and a complete-but-empty form are both excluded and counted in the gap instead ("9 of 10 booked tests produced no result"), while a test whose DATA is impossible keeps its row because an error is data; every result read off a curve carries its plot as a figure, and a test with no chart (a water content) is still tabulated; the figure is asserted to be the SAME `Drawing` the test card renders, since `labChart` is the one mapping |
| Coulomb & Mononobe–Okabe earth pressure | `coulomb.test.ts` | Ka and Kp reduce EXACTLY to Rankine at δ = θ = β = 0 (to 1e-12) — the cheapest check the expression is transcribed right; wall-friction and sloping-fill hand calcs (Ka = 0.29731 at φ 30°/δ 20°, 0.34002 at β 10°); Kp overestimate vs Rankine flagged rather than hidden; M–O collapses to static Coulomb as kh → 0, Kae = 0.45396 at kh 0.2, increment at 0.6H per Seed & Whitman, and NO number returned when φ − β − ψ < 0; `validation.ts` `coulomb-reduces-to-rankine`/`coulomb-ka-wall-friction`/`mononobe-okabe-kae` |
| Bearing capacity — general equation | `bearingGeneral.test.ts`, `geotech.test.ts` | Nγ vs the PUBLISHED tables for all three methods at φ = 30° and 35° (Meyerhof 15.668/37.152, Hansen 15.070/33.921, Vesić 22.402/48.029); full hand calc of q_u for a 2×2 m footing at Df = 1.5 m (1553.72 kPa); Das three-case water-table correction incl. the interpolated γ̄; Meyerhof effective area under eccentricity; iγ = 0 once β ≥ φ; net-vs-gross FS handled explicitly; `validation.ts` `bearing-ngamma-vesic`/`-meyerhof`/`bearing-general-qult`. The older mixed-method `bearingCapacity()` was DELETED (it combined a Vesić Nγ, a Meyerhof s_c, no s_q and a De Beer s_γ); a test now asserts s_q is applied, since omitting it understated an embedded square footing by ~17% |
| Soils — chart geometry | `chartKit` via `lab/plots.test.ts`, `liquefactionPlot.test.ts` | shared axis furniture: 1–2–5 round ticks (nearest, not up), equal decades occupy equal width on log axes, annotation plates opaque and inside the frame, nothing drawn outside the plot area, FS shading stopped at the base of the hole |
| Soils — parameters & provenance | `parameters.test.ts`, `provenance.test.ts`, `registry.test.ts`, `validate.test.ts`, `store.test.ts` | evidence hierarchy (measured > derived > correlated > assumed) picks the source, never averages them; every registry entry carries an equation, units, symbols with units, and a standard or published reference; validation separates impossible (PL > LL, overlapping layers) from unusual (N = 2, low recovery) and never mutates |
| Wind | `wind.test.ts` | §207B qz vs hand calc (`validation.ts` `wind-qz`) |
| Loads & tributary | `loads.test.ts`, `deadLoads.test.ts`, `liveLoads.test.ts`, `tributary.test.ts`, `thermalLoad.test.ts` | NSCP 204/205 table lookups; tributary-area statics; EA·α·ΔT |
| Model / bridge / mesh | `modelBridge.test.ts`, `meshValidation.test.ts`, `modelBuilder.test.ts`, `rigidEndZones.test.ts`, `diaphragm.test.ts` | section-property hand calcs (incl. Timoshenko shear areas), constraint kinematics, validation rules |
| Design pipeline | `pipeline.test.ts`, `beamSections.test.ts`, `barSelectionAndGamma.test.ts` | envelope semantics, optimizer termination, bar-continuity guards |
| Trusses | `truss.test.ts`, `trussDesign.test.ts`, `trussTakeoff.test.ts` | method-of-joints statics vs solver |
| Quantities | `takeoff.test.ts`, `quantities.test.ts` | hand-computed BOM quantities |
| Numerics | `fem.test.ts`, `math.test.ts`, `modal.test.ts` (Jacobi) | LU round-trips; eigen closed forms (`validation.ts` `eigen-jacobi`) |

---

# Open items (need resources outside this repo)

| ID | What | Blocker |
|----|------|---------|
| X001 | ETABS space-frame cross-check (F3D002, RS002, M003, B006) | needs an ETABS license + exported reference results checked into `docs/benchmarks/` |
| X002 | STAAD space-frame cross-check (FR004, F3D003) | ✅ **done, 2026-08-19.** `docs/benchmarks/staad-gridframe-2026-08-19.anl` vs `gridframe.model.json` — 2×1-bay, 2-storey, slab meshed 10×10, with **every input matched**: geometry, sections, E = 24 870 MPa, ν = 0.20, γ = 23.5615 kN/m³, D = 1380.39 kN, L = 288.00 kN. Nothing normalised. Total reaction **2117.27 vs 2117.28 kN (0.001 %)**; joint deflections agree to **0.16 % on the mean** (±4 % joint by joint); support reactions to **±3 %**; column carry-over 0.510 vs 0.488 against the theoretical 0.500. This cross-check found the `frame3d` My sign defect (#614). **Separately recorded, not a solver disagreement:** the tributary load model vs a meshed slab — long-span beams +7…11 % (conservative), edge girders −10…19 %, interior girders −22…29 %, column moments −25…49 %, beam torsion 0.4 vs 14.6 kN·m. Surfaced in the app on the Analysis options card. |
| X003 | PCA Column biaxial + slender cross-check (C003, C004) | needs spColumn reference curves |
| X004 | Excel verification sheets (Roadmap Phase-2 goal) | authoring task — the `/validation` page already renders manual-vs-software tables that can seed them |
| T-beam flexure (`tbeam.ts`) | §6.3.2 bf table + two-couple T flexure vs hand calc (Asf 1290 mm², rect/true-T switch, εt/φ) | ✅ `tbeam.test.ts` (14) |
| Flanged action in `beamDesign.ts` | §406.3.2 branch (rectangle b_f while a ≤ h_f, else the two-couple true T), T vs L from the adjoining slab panels, adoption on TOTAL steel | ✅ `beamDesign.test.ts` (10), `pipeline.test.ts` (3), `modelReport.test.ts` (2) |
| Slab cage (`slabCage.ts`) | both mats from the panel's DDM strips + ACI Fig. 8.7.4.1.3 extensions, edge continuity, chairs only under top steel, embedment clamped to the supporting beam's own width, mats lapped at stock length, top mat bearing on the beams' top bars, bottom bar hooked up onto them at a free edge, extra top bars interleaved onto the bottom mat's grid | ✅ `slabCage.test.ts` (56), `cageBuilder.test.ts` (11) |
| Prestressed beam (`prestressedBeam.ts`) | PCI losses, §24.5 stress limits, fps §20.3.2.3.1, 1.2Mcr, Vci/Vcw vs hand calc | ✅ `prestressedBeam.test.ts` (12) |
